.container.white
  .row
    .col-xs-12
      %h1 Edit '#{@car.name}'
      %p Keep the deets up to date
      = simple_form_for @car, :html => {:class => "form-horizontal", :multipart => true, "role" => "form"}, :defaults => { :input_html => {:class => "form-control"}} do |f|
        = field_set_tag "Car Info" do
          = f.input :name
          = f.input :number, :label => "Car number"
          = f.input :image, :label => "Car photo"
          = f.input :description, :placeholder => "What makes this ride different? Would xzibit want to pimp it, or just buy you a new one like in that episode where the kid had the two half-cars welded together?", :as => :text, :input_html => { :rows => 10 }
        = f.button :submit
